@using System.Reflection;
@using System.ComponentModel.DataAnnotations
@attribute [Display(Name ="字符串属性")]
@typeparam TModel
@inherits PropStringBase<TModel>
@if (Value != null)
{
    <span style="color:red"> @Property.GetValue(Value)</span>
}